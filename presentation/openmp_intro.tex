\documentclass[handout]{beamer}

%\let\Tiny\tiny% http://tex.stackexchange.com/a/94159/5764
\setbeamertemplate{navigation symbols}{}

%\usepackage[outputdir=build]{minted}
\definecolor{subtlegray}{rgb}{0.92,0.92,0.92}

\PassOptionsToPackage{outputdir=build}{minted}
\usepackage{tcolorbox}
\tcbuselibrary{minted,skins}

\newtcblisting{ccode}[1]{
    listing engine=minted,
    colback=subtlegray,
    %colframe=black!70,
    listing only,
    minted style=default,
    minted language=c,
    minted options={fontsize=\footnotesize,#1}
}

\newtcbinputlisting{\tcbcode}[2]{%
    listing engine=minted,
    colback=subtlegray,
    %colframe=black!70,
    listing only,
    minted style=default,
    minted language=#1,
    minted options={fontsize=\footnotesize},
    listing file={#2}
}


\definecolor{bg}{rgb}{0.85,0.85,0.85}

\title{Introduction to OpenMP}
\subtitle{Ferienakademie 2017}
\author{Nathan Brei}
\institute{}
\date\today

\begin{document}
\begin{frame}
  \titlepage
\end{frame}

\begin{frame}[fragile]
  \frametitle{Launching a team of threads --- Parallel Regions}
  \begin{columns}[t]%[onlytextwidth]
    \begin{column}{0.65\textwidth}
      \tcbcode{c}{src/ex1.c}
    \end{column}
    \pause
    \begin{column}{0.4\textwidth}
      \tcbcode{text}{output/ex1.txt}
      \begin{ccode}[gobble=8]{text}
        Output gets
        Way weirder than what
        is shown just because
        CPUs are complicated
      \end{ccode}
    \end{column}
  \end{columns}
  \pause
  \begin{itemize}
  \item \emph{Fork-join execution model}
  \item Print statements are interleaved nondeterministically
  \end{itemize}
\end{frame}


\end{document}
